[package]
name = "multimodal-core"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
description = "Deterministic multimodal signal primitives for the Advanced Multi-modal AI repository."

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
sha2 = "0.10"

[dev-dependencies]
approx = "0.5"
