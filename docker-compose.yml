services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    command: uvicorn advanced_multimodal_ai.api:create_app --factory --host 0.0.0.0 --port 8000
    ports:
      - "8000:8000"
